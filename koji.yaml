# Koji cluster configuration
# See docs/configuration.md for full reference

project: koji-dev

cluster:
  name: default
  base_port: 9400  # dashboard at :9400, server at :9401, services auto-assign from :9410+

# services:
#   parse: true     # Set to false to skip the parse service (e.g., extracting from pre-parsed markdown)
#   ollama: true    # Set to false to skip ollama (e.g., using OpenAI only)

pipeline:
  - step: parse
    engine: docling

  - step: extract
    # Use openai/gpt-4o-mini for quick start (set OPENAI_API_KEY env var)
    # Use ollama models (e.g., llama3.2) for fully local processing
    model: openai/gpt-4o-mini
    schemas:
      - ./schemas/examples/invoice.yaml

output:
  structured: ./output/
